# .github/settings.yml
repository:
  description: "Offline CI director that compiles sensor receipts into deterministic merge evidence and stable PR surfaces."
  homepage: "https://github.com/EffortlessMetrics/cockpitctl"
  topics: rust, ci, sensor-receipts, static-analysis, policy, merge-surface, deterministic, evidence

<!-- cockpit:begin -->
## Cockpit

This comment is generated by `cockpitctl`. Details live in `artifacts/`.

### Summary

| Sensor | Status | Blocking | Notes |
|---|---:|---:|---|
| `bad-sensor` | ❌ fail | yes | `artifacts/bad-sensor/report.json` |

### Highlights

(showing up to **7**)

1. ❌ **bad-sensor**: `cockpit.schema_violation` — Receipt for sensor `bad-sensor` at `artifacts/bad-sensor/report.json` does not conform to sensor.report.v1 schema: 5 schema violations: /findings/0: "code" is a required property; /findings/0: "message" is a required property; /run: "started_at" is a required property; /tool: "version" is a required property; /verdict: "counts" is a required property

### Annotations

1. ❌ **bad-sensor**: `cockpit.schema_violation` — Receipt for sensor `bad-sensor` at `artifacts/bad-sensor/report.json` does not conform to sensor.report.v1 schema: 5 schema violations: /findings/0: "code" is a required property; /findings/0: "message" is a required property; /run: "started_at" is a required property; /tool: "version" is a required property; /verdict: "counts" is a required property

### Validation

- `bad-sensor`: ❌ fail · report `artifacts/bad-sensor/report.json`
  - repro: `bad-sensor check --verify`

<!-- cockpit:end -->

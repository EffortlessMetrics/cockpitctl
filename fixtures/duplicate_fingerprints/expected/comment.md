<!-- cockpit:begin -->
## Cockpit

This comment is generated by `cockpitctl`. Details live in `artifacts/`.

### Summary

| Sensor | Status | Blocking | Notes |
|---|---:|---:|---|
| `dupescanner` | ❌ fail | yes | `artifacts/dupescanner/report.json` |

### Highlights

(showing up to **7**)

1. ❌ **dupescanner**: `dupescanner.duplicate_code` at `src/bar.rs:20` — Duplicate code block detected (same fingerprint)
2. ⚠️ **dupescanner**: `dupescanner.similar_code` at `src/alpha.rs:5` — Similar code pattern found
3. ⚠️ **dupescanner**: `dupescanner.no_fingerprint` at `src/delta.rs:40` — This finding has no fingerprint and will get derived one
4. ⚠️ **dupescanner**: `dupescanner.unique_finding` at `src/gamma.rs:30` — This finding has a unique fingerprint
5. ℹ️ **dupescanner**: `cockpit.receipt_inconsistent` — Receipt counts for sensor `dupescanner` did not match findings: reported info=0, warn=4, error=1, computed info=0, warn=4, error=2.

### Annotations

1. ❌ **dupescanner**: `dupescanner.duplicate_code` at `src/bar.rs:20` — Duplicate code block detected (same fingerprint)
2. ⚠️ **dupescanner**: `dupescanner.similar_code` at `src/alpha.rs:5` — Similar code pattern found
3. ⚠️ **dupescanner**: `dupescanner.no_fingerprint` at `src/delta.rs:40` — This finding has no fingerprint and will get derived one
4. ⚠️ **dupescanner**: `dupescanner.unique_finding` at `src/gamma.rs:30` — This finding has a unique fingerprint
5. ℹ️ **dupescanner**: `cockpit.receipt_inconsistent` — Receipt counts for sensor `dupescanner` did not match findings: reported info=0, warn=4, error=1, computed info=0, warn=4, error=2.

### Policy

- `dupescanner`: ❌ fail · report `artifacts/dupescanner/report.json`
  - repro: `dupescanner check`

<!-- cockpit:end -->

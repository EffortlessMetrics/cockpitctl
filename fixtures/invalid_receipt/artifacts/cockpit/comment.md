<!-- cockpit:begin -->
## Cockpit

This comment is generated by `cockpitctl`. Details live in `artifacts/`.

### Summary

| Sensor | Status | Blocking | Notes |
|---|---:|---:|---|
| `builddiag` | ✅ pass | yes | `artifacts/builddiag/report.json` |
| `malformed` | ❌ fail | yes | `artifacts/malformed/report.json` |

### Highlights

(showing up to **7**)

1. ❌ **malformed**: `cockpit.invalid_receipt` — Invalid receipt for sensor `malformed` at `artifacts/malformed/report.json`: key must be a string at line 1 column 3

### Annotations

1. ❌ **malformed**: `cockpit.invalid_receipt` — Invalid receipt for sensor `malformed` at `artifacts/malformed/report.json`: key must be a string at line 1 column 3

### Repo contract

- `builddiag`: ✅ pass · report `artifacts/builddiag/report.json`
  - repro: `builddiag check --profile team`

### Policy

- `malformed`: ❌ fail · report `artifacts/malformed/report.json`
  - repro: `malformed check --base $BASE_SHA`

<!-- cockpit:end -->

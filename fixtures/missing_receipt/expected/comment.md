<!-- cockpit:begin -->
## Cockpit

This comment is generated by `cockpitctl`. Details live in `artifacts/`.

### Summary

| Sensor | Status | Blocking | Notes |
|---|---:|---:|---|
| `builddiag` | ✅ pass | yes | `artifacts/builddiag/report.json` |
| `diffguard` | ❌ fail | yes | `artifacts/diffguard/report.json` |

### Highlights

(showing up to **7**)

1. ❌ **diffguard**: `cockpit.missing_receipt` — Expected receipt for sensor `diffguard` but it was not found at `artifacts/diffguard/report.json`.

### Repo contract

- `builddiag`: ✅ pass · report `artifacts/builddiag/report.json`
  - repro: `builddiag check --profile team`

### Policy

- `diffguard`: ❌ fail · report `artifacts/diffguard/report.json`
  - repro: `diffguard check --base $BASE_SHA --head $HEAD_SHA`

<!-- cockpit:end -->

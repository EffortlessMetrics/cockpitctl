<!-- cockpit:begin -->
## Cockpit

This comment is generated by `cockpitctl`. Details live in `artifacts/`.

### Summary

| Sensor | Status | Blocking | Notes |
|---|---:|---:|---|
| `builddiag` | ✅ pass | yes | `artifacts/builddiag/report.json` |
| `diffguard` | ⚠️ warn | yes | `artifacts/diffguard/report.json` |

### Highlights

(showing up to **7**)

1. ⚠️ **diffguard**: `diffguard.forbidden_api` at `src/lib.rs:42` — Forbidden API introduced: std::mem::transmute

### Annotations

1. ⚠️ **diffguard**: `diffguard.forbidden_api` at `src/lib.rs:42` — Forbidden API introduced: std::mem::transmute

### Repo contract

- `builddiag`: ✅ pass · report `artifacts/builddiag/report.json`
  - repro: `builddiag check --profile team`

### Policy

- `diffguard`: ⚠️ warn · report `artifacts/diffguard/report.json`
  - repro: `diffguard check --base $BASE_SHA --head $HEAD_SHA`

<!-- cockpit:end -->

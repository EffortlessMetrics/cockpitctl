# cockpitctl-exec

Process-execution adapters for cockpitctl runtime extensions:

- post-processing hooks (`run_hooks`)
- buildfix actuator invocation (`run_buildfix_actuator`)
- policy signing key resolution (`load_policy_signing_key`)

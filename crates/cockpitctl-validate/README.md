# cockpitctl-validate

Schema-validation adapters for cockpitctl.

- `JsonSchemaValidator` implementing `cockpitctl_ingest::SchemaValidator`
- embedded `sensor.report.v1` and `cockpit.report.v1` schemas via `cockpitctl-types`

# cockpitctl-validate

Focused validation microcrate for checking JSON files against `sensor.report.v1` and
`cockpit.report.v1` contracts.

This crate exists to keep CLI concerns (argument parsing and process IO) separate from
validation behavior.

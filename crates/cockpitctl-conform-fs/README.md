# cockpitctl-conform-fs

Filesystem orchestration helpers for conformance checking.

This crate bridges on-disk artifacts (`artifacts/<sensor_id>/report.json`) to
`cockpitctl-conform`'s pure validation functions.

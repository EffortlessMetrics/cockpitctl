# cockpitctl-render-annotations

Single-responsibility crate for rendering cockpit highlight annotations:

- Markdown annotation lists (`render_annotations`, `render_annotations_section`)
- GitHub Actions workflow command annotations (`render_github_annotations`)

Ordering and capping contracts are preserved from the previous `cockpitctl-render` implementation.

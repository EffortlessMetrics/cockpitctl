# cockpitctl-render-annotations

Focused annotation renderers used by `cockpitctl-render`:

- Markdown annotation lists for PR comments
- GitHub Actions workflow command annotations

Both renderers preserve deterministic ordering and enforce policy capping.

[package]
name = "cockpitctl-render"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
authors.workspace = true
description = "PR comment renderer with budgeting and stable markers"
readme = "../../README.md"
keywords = ["ci", "pr-comment", "markdown", "rendering"]
categories = ["development-tools", "text-processing"]

[dependencies]
cockpitctl-types = { path = "../cockpitctl-types", version = "0.1.0" }

[dev-dependencies]
criterion.workspace = true
rand.workspace = true

[[bench]]
name = "render_benches"
harness = false
